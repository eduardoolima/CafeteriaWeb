@using CafeteriaWeb.ViewModel;
@model ShoppingCartViewModel

@* @if(Model.ShoppingCart.ShoppingCartItems.Count > 0)
{ *@
    <a asp-controller="ShoppingCart" asp-action="Index">
        <div style="position: relative; display: inline-block;">
            <span style="color: white; font-size: 18px;">
                <i class="fa-solid fa-cart-shopping"></i>
            </span>
            @if(Model.ShoppingCart.ShoppingCartItems.Count > 0)
            {
                <span style="color: white; position: absolute; top: -8px; right: -8px; width: 18px; height: 18px; background-color: red; border-radius: 50%; text-align: center; line-height: 18px; font-size: 12px;">
                    <span style="display: inline-block; vertical-align: top;">@Model.ShoppingCart.ShoppingCartItems.Count</span>
                </span>        
            }
        </div>
    </a>
@* } *@
